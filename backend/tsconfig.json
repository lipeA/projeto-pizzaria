{
  "compilerOptions": {
    /* ================================
       📂 Estrutura de pastas
    ================================= */
    "rootDir": "./src", // Código-fonte TypeScript
    "outDir": "./dist", // Saída compilada em JS

    /* ================================
       🌐 Ambiente
    ================================= */
    "target": "ES2020", // Versão moderna do JS
    "module": "commonjs", // Compila para CommonJS (evita problemas de ESM)
    "moduleResolution": "node", // Resolve pacotes como Node.js
    "lib": ["ES2020"], // APIs modernas do ECMAScript
    "types": ["node"], // Tipos do Node.js
    "esModuleInterop": true, // Permite import default de libs CommonJS

    /* ================================
       🛠 Saída e Debug
    ================================= */
    "sourceMap": true, // Gera mapas para debug
    "declaration": true, // Gera arquivos .d.ts para tipagem
    "declarationMap": true, // Mapas para .d.ts

    /* ================================
       🔒 Regras de segurança de tipos
    ================================= */
    "strict": true, // Ativa todas as verificações estritas
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,

    /* ================================
       🚀 Otimizações
    ================================= */
    "skipLibCheck": true, // Ignora checagem de libs externas
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
